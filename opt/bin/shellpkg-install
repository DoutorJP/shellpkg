#!/bin/bash

if [ "$*" == "" ]; then
    echo "No package selected."
    exit 1
fi

echo "Installing $1:"
#TODO: check for dependencies and conflicts
read -n1 -p "Press any key to continue or ^c to abort."
exec /opt/shellpkg-repo/$1/install.sh

echo "Unknown package: $1"
exit 1
